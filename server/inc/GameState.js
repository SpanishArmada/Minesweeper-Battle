module.exports = function (players, board) {
	var players = players,
		hiddenBoard = board;

	// gameBoard contains revealed grids
	var gameBoard = null;

	function clickReveal(i, j) {

	}

	function clickFlag(i, j) {

	}
}